{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Full App (Frontend & Backend)",
            "type": "node",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Run All Servers"
        }
    ]
}
